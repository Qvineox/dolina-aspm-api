// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: defects/v1/fix.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "dolina.defects.v1";

export interface FixInfo {
  description: string;
  updateVersionList: FixedVersions[];
  downgradeVersionList: FixedVersions[];
}

export interface FixedVersions {
  updateVersionList: string[];
}

function createBaseFixInfo(): FixInfo {
  return { description: "", updateVersionList: [], downgradeVersionList: [] };
}

export const FixInfo: MessageFns<FixInfo> = {
  encode(message: FixInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.description !== "") {
      writer.uint32(10).string(message.description);
    }
    for (const v of message.updateVersionList) {
      FixedVersions.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.downgradeVersionList) {
      FixedVersions.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FixInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFixInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.updateVersionList.push(FixedVersions.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.downgradeVersionList.push(FixedVersions.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FixInfo {
    return {
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      updateVersionList: globalThis.Array.isArray(object?.updateVersionList)
        ? object.updateVersionList.map((e: any) => FixedVersions.fromJSON(e))
        : [],
      downgradeVersionList: globalThis.Array.isArray(object?.downgradeVersionList)
        ? object.downgradeVersionList.map((e: any) => FixedVersions.fromJSON(e))
        : [],
    };
  },

  toJSON(message: FixInfo): unknown {
    const obj: any = {};
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.updateVersionList?.length) {
      obj.updateVersionList = message.updateVersionList.map((e) => FixedVersions.toJSON(e));
    }
    if (message.downgradeVersionList?.length) {
      obj.downgradeVersionList = message.downgradeVersionList.map((e) => FixedVersions.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FixInfo>, I>>(base?: I): FixInfo {
    return FixInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FixInfo>, I>>(object: I): FixInfo {
    const message = createBaseFixInfo();
    message.description = object.description ?? "";
    message.updateVersionList = object.updateVersionList?.map((e) => FixedVersions.fromPartial(e)) || [];
    message.downgradeVersionList = object.downgradeVersionList?.map((e) => FixedVersions.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFixedVersions(): FixedVersions {
  return { updateVersionList: [] };
}

export const FixedVersions: MessageFns<FixedVersions> = {
  encode(message: FixedVersions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.updateVersionList) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FixedVersions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFixedVersions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.updateVersionList.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FixedVersions {
    return {
      updateVersionList: globalThis.Array.isArray(object?.updateVersionList)
        ? object.updateVersionList.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: FixedVersions): unknown {
    const obj: any = {};
    if (message.updateVersionList?.length) {
      obj.updateVersionList = message.updateVersionList;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FixedVersions>, I>>(base?: I): FixedVersions {
    return FixedVersions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FixedVersions>, I>>(object: I): FixedVersions {
    const message = createBaseFixedVersions();
    message.updateVersionList = object.updateVersionList?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
